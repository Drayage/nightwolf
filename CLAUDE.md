# 붉은달의 의식 — 눈먼 장로가 되어 마을에 숨어든 배신자를 찾아내는 원나잇 웨어울프 로그라이크

바닐라 JS / PWA / Firebase 온라인 미사용(구조만 준비) / GitHub Pages 배포

## 파일 지도

- `js/engine.js` — 게임 규칙/상태 (순수 함수, DOM 없음). `scripts/simulate.mjs`가
  헤드리스로 돌리는 대상. 모든 무작위성은 인자로 받은 `rng`로만 쓴다.
- `js/ai.js` — 장로 의사결정 정책 (시뮬레이터용이자 향후 봇전 모드의 기반)
- `js/data/relics.js` — 유물/이름/증언/스토리 텍스트 (콘텐츠 수정은 여기)
- `js/ui.js` — DOM 렌더 (타이틀/하루 진행/종료 화면)
- `js/net.js` — 온라인 동기화 배관(RTDB, `redmoon_rooms/` 네임스페이스). 현재
  장로 모드는 싱글플레이라 미사용 — 봇전/멀티 모드를 붙일 때 연결
- `js/storage.js` — 새로고침 복원
- `js/audio.js` / `js/palettes.js` — 사운드 엔진과 팔레트(RITUAL) 분리
- `sw.js` — PWA 캐시 (network-first + CACHE_VERSION + controllerchange 1회 reload)
- `scripts/simulate.mjs` — headless 시뮬레이터 (AI/밸런스/라운드 플로우 검증)
- `tools/gen-icons.js` — PWA 아이콘 생성 스크립트

## 게임 규칙 요약 (엔진 로직의 근거)

- 마을 11명 + 유물함(중앙 유물 풀, 초기 3장). 관련 룰북: 원나잇 얼티메이트 웨어울프
  (한국어판) — 단, "하룻밤 한정"이던 원작 능력들을 "매일 밤 반복"으로 확장했다.
- **마을 구성은 매 런 무작위다**(`js/engine.js`의 `generateRelicLayout`). 제물/하수인만
  정확히 1명씩 고정이고, 결계는 항상 짝수(0/2/4, 가중치상 2가 가장 흔함), 나머지 칸은
  예지/도둑/문제아/취객/광기/평범 중에서 칸마다 독립적으로 뽑는다(`DUPLICATABLE_RELICS`)
  — 그래서 어떤 유물이 아예 안 나올 수도, 여러 명 겹칠 수도 있다. `js/data/relics.js`의
  `ROLE_SLOTS`는 이제 "항상 그 인원수만큼만 나온다"는 보장이 아니라 봇 추리용 대충의
  기본값일 뿐이다. **이 때문에 밤 캐스케이드 함수(`nightMason`/`nightSeer`/`nightRobber`/
  `nightTroublemaker`/`nightDrunk`)는 전부 그 유물을 쥔 사람 전원을 순회한다** — 예전에
  `.find()`로 단 한 명만 처리하던 코드가 남아있으면 중복 인원의 belief가 끝내 안 채워져
  낮 증언에 "undefined의 유물을 봤는데 알 수 없는 유물이었어요"처럼 깨져 나오는 버그가
  났다(결계도 마찬가지 이유로 홀수 인원만 짝짓고 나머지를 방치하면 안 됨 — 반드시 전원
  짝짓거나 짝 없음으로 명시 처리).
- **두 층위의 진실이 있다.** `villager.relic`은 기계적으로 "지금 실제 정체"(위협
  판정·다음 밤 행동을 여기로 결정), `villager.belief`는 "본인이 기억하는 자기 모습"
  (낮 증언은 여기서 나옴). 유물은 주머니 속에 있어 아무도 지금 자기가 뭔지 안 보고,
  자기가 직접 행동했을 때만 belief가 갱신된다 — 그래서 말하는 유물과 실제 유물이
  다를 수 있다(도둑맞은 쪽/문제아에게 휘말린 쪽은 자기가 바뀐 줄도 모른 채 예전 belief
  그대로 계속 말함). 제물/하수인은 `claimRole`(사칭 대상, 한 번 정해지면 고정)이라는
  별도의 거짓 belief(`fakeBelief`)를 쓴다 — 근거는 매일 새로 지어낸다(로버 사칭만
  예외 — "모른다"가 원래 정답이라 지어낼 필요가 없음).
- 밤 캐스케이드는 원작 기상 순서(비밀요원→천리안→도둑→문제아→취객, `js/engine.js`의
  `night*` 함수)대로 진행된다 — 순서가 실제로 중요하다: 천리안이 본 뒤에 도둑이
  그 사람을 훔쳐갈 수도 있고, 그러면 천리안의 belief는 그대로인데 relic만 바뀐다.
  도둑은 무작위 한 명과 유물 통째로 교환(belief: 누구와 바꿨는지만 앎, 결과는 모름),
  문제아는 자신을 뺀 다른 두 명의 유물을 교환(자기 유물은 유지, 매일 새로 두 명 골라
  belief 갱신), 취객은 자기 유물을 유물함에 넣고 무작위로 하나 뽑음(무엇을 받았는지
  모름). 처형된 사람의 유물도 유물함으로 돌아가 계속 순환한다 — 제물/하수인도
  예외 없이 도둑/문제아/취객을 거치며 다른 사람에게 넘어갈 수 있다("지금" 누가
  위험한지는 여전히 `relic` 하나로만 판정 — 처형 성공, 승리 조건, 하수인의 제물
  심기 자격 전부 `relic` 기준).
- **"누가 그 능력을 쓰는가"는 그날 밤 시작 시점의 유물 스냅샷(`actingRelic`,
  `runNightPhase`에서 캐스케이드 돌기 전에 한 번 뜸) 하나로 고정한다** — 다 같이
  동시에 능력을 쓰고 카드 이동(실제 relic 교환)만 순서대로 처리한다고 생각하면
  된다. 그래서 도둑이 문제아의 유물을 훔쳐가도 그 도둑은 그날 밤 문제아 능력까지
  이어서 쓰지 않고(원래 자기 도둑 능력만 씀), 반대로 문제아 쪽에서 유물을 뺏긴
  사람도 스냅샷에 문제아로 찍혀 있으니 그날 밤 예정대로 문제아 능력을 쓴다(카드는
  이미 넘어갔어도 — 그 능력이 건드리는 대상들의 relic은 여전히 실시간 값이라, 방금
  자기 카드를 훔쳐간 사람이 문제아의 대상으로 다시 걸릴 수도 있다). `night*` 함수들
  이 `v.relic` 대신 `actingRelic.get(v.id)`로 행위자를 고르는 이유가 이거다 — 실제
  교환 로직(누구 relic이 뭘로 바뀌는지)은 여전히 실시간 값을 쓴다.
- **relic은 유지되지만 belief는 매일 밤 새로 생긴다.** 다들 밤이 시작되기 전에 자기
  유물을 스스로 확인하고 들어간다(`syncPassiveBelief` + `refreshFakeBeliefs`가 이
  원리를 함께 구현). 그래서:
  - 능동 유물(예지/도둑/문제아/취객)을 쥔 사람은 그날 밤 실제로 그 능력을 쓰고
    belief가 그 행동으로 갱신된다.
  - 평범/광기처럼 능력이 없는 유물은 숨길 것도 없으니 확인한 그대로 믿는다 —
    도둑/문제아한테 능동 유물을 뺏겨 평범/광기가 된 사람은 바로 다음 날부터
    "저는 평범한 유물이에요"로 정확히 말한다.
  - **제물/하수인도 마찬가지다.** `isThreat()`가 startingRelic이 아니라 `v.relic`을
    그대로 본다 — 그 유물을 얼떨결에 넘겨받은 사람은 그날 밤 확인하는 순간 "헉,
    내가 제물?"하며 그때부터 거짓말을 시작하고(`fakeBelief`), 반대로 도둑/문제아
    한테 그 유물을 뺏긴 사람은 "나 이제 아니네" 하고 안심하며 다시 진실을 말한다.
    제물/하수인 유물 자체는 도둑·문제아·취객을 거치며 계속 다른 사람에게 넘어갈
    수 있다 — "지금" 누가 위험한지, 처형 성공 여부, 승리 조건, 하수인의 제물
    심기 자격 전부 `relic` 하나로 판정.
  - `syncPassiveBelief`를 캐스케이드 맨 앞에서 돌리는 이유는, 그날 밤 실제로 능동
    유물을 얻어 행동하게 되면(예: 확인 직후 도둑맞아 결계가 된 경우) 뒤이은
    night*가 그 belief를 자연스럽게 다시 덮어써야 하기 때문이다.
- **belief에는 언제 그 일이 있었는지(`day`)도 찍힌다.** 예지/도둑/문제아/취객은 "어젯밤
  ~썼어요"라고 과거형으로 말하는데, 위 자기확인 덕분에 대부분은 항상 최신이지만
  아주 드물게(그날 밤 유효한 대상이 아무도 없어 행동을 못 한 경우 등) belief가 여러
  날 묵을 수 있다 — 그런 낡은 belief까지 "어젯밤"이라고 하면 다른 증거와 모순될 수
  있어, `CLAIM_LINES`의 `recency(belief, day)`가 `belief.day === 오늘`이면 "어젯밤",
  아니면 "예전에"로 갈라 말한다. 사칭(`fakeBelief`)은 매일 새로 지어내므로 항상
  오늘 날짜가 찍혀 늘 "어젯밤"으로 말한다.
- **밤과 낮은 실제로 분리된 페이즈다**(`state.phase`). 밤에는 캐스케이드와 그날의
  belief/증언이 전부 미리 계산되어 `state.pendingClaims`에 담기지만 아직 공개되지
  않는다 — 플레이어는 그날 밤 **딱 한 명만** 짚어 귀를 기울일 수 있다(`listenTo()`,
  `state.nightListenedId`로 잠금 — 한 번 고르면 다른 사람은 더 짚을 수 없고, 이미
  고른 사람을 다시 눌러도 소리만 재생). 그 사람이 오늘 밤 능력에 얽혔으면(행위자든
  대상이든, `state.nightInvolvement`) 소리와 함께 `nightListenLine()` 글도 로그에
  남는다. "낮이 밝았다"를 누르면 `revealDay()`가 pendingClaims를 log에 공개하고
  phase를 'day'로 바꾼다. 예지의 유물 증언은 "평범하다/이상하다" 같은 판정이 아니라
  실제로 본 유물 이름을 그대로 말한다(`belief.targetRelic` → `CLAIM_LINES.seer`).
- 매일 처형은 필수. 그날 처형 대상이 `relic==='sacrifice'`가 아니면(광기의 유물
  예외 있음, 아래) 즉시 패배. 투옥은 선택이며 되돌릴 수 있음.
- **감옥은 한 자리뿐**(`jail()`이 새 대상을 가두면 이전 수감자를 자동 석방). 갇힌
  동안은 도둑/문제아/취객의 밤 캐스케이드가 그 사람을 건너뛴다(이미 `!v.jailed`로
  체크 중).
- **제물은 여러 명일 수 있다.** 처형이 진짜 성공이든, 광기의 유물을 처형해 "성공한
  것처럼 보이는" 위장이든 — 그 처형 직후 하수인이 갇혀있지 않으면 무작위 생존자
  한 명의 유물을 `'sacrifice'`로 바꿔(원래 유물은 유물함으로) 제물을 하나 더 심는다.
  광기의 유물은 실제로는 아무 제물도 제거하지 못하면서 장로에게는 성공처럼 보이므로,
  하수인 입장에선 들키지 않고 위협을 늘릴 절호의 기회다.
- **처형은 즉시 결과가 뜨지 않는다.** `execute()`는 "장로가 OO을(를) 제물로 처형했다"
  로그만 남기고 `phase: 'executed'`로 멈춘 뒤, 실제 판정(성공/실패/광기 위장)을
  `pendingResolution`에 감춰둔다 — 플레이어가 "계속"을 눌러 `continueAfterExecution()`
  을 호출해야 비로소 그 결과가 드러난다(로그 배열은 반드시 복제해서 넘겨야 감춰진
  단계에서 아직 안 보여야 할 내용이 새지 않는다). 봇/시뮬레이터는 `stepGame()`이
  `execute()` 직후 곧바로 `continueAfterExecution()`을 호출해 이 연출 대기를 건너뛴다.
- 승리: 살아있는 `relic==='sacrifice'` 소지자가 0명이고 하수인이 갇혀있거나 죽었을 때
  — 조용히 다음 밤으로 넘어간다(연출 없음). 잘못된 처형은 `RITUAL_FAILURE_TEXT` 연출
  (붉은달·주문·늑대) 이후 `state.revealTable`(사람마다 시작 유물/최후 유물 2열)이
  있는 결말 화면으로 이어진다. 최대 7일 안에 못 끝내도 패배(연출 없이
  TIMEOUT_FAILURE_LINE + revealTable).
- 사냥꾼은 제거했다(투표가 장로 1인뿐이라 "사냥꾼이 지목한 자도 죽는다"는 원작 효과가
  성립하지 않음). 무두장이는 "광기의 유물"로 개명하고 위 위장 메커닉으로 대체했다.

## 명령어

- 로컬 서버: `python3 -m http.server`
- 시뮬: `node scripts/simulate.mjs [판수]` (기본 500판, 교착 0이어야 함)
- 사운드 미리듣기: `audio-preview.html`을 로컬 서버로 열기

## 규칙

- 처음부터 포함: 상대경로, network-first SW, 세션 복원, `redmoon_` DB 네임스페이스
  (나중에 얹으면 매번 같은 버그가 났다 — game-baserule 12개 저장소 교훈)
- 장로 정책(js/ai.js)이나 밤 페이즈 판정 로직을 수정하면 시뮬레이터부터 돌려서
  교착 0 / 승률이 0%나 100%로 붕괴하지 않았는지 확인한 뒤 커밋
- UI 문자열/새 CSS 클래스는 구체적 이름 사용 (일반명 충돌 전례)
- 커밋 전 검증·SW·모바일: webgame-ship 스킬 / 온라인: firebase-online 스킬 참조
  (둘 다 `drayage-kit` 마켓플레이스의 game-kit 플러그인으로 제공됨)

## 아직 안 한 것

- 불면증환자/도플갱어 (나머지 두 원작 역할)
- 봇전 모드 (js/ai.js의 정책을 상대 봇으로 재사용 예정)
- 런 종료 후 메타 진행(장로의 유산) — 로그라이크 언락 시스템
