# 붉은달의 의식 — 눈먼 장로가 되어 마을에 숨어든 배신자를 찾아내는 원나잇 웨어울프 로그라이크

바닐라 JS / PWA / Firebase 온라인 미사용(구조만 준비) / GitHub Pages 배포

## 파일 지도

- `js/engine.js` — 게임 규칙/상태 (순수 함수, DOM 없음). `scripts/simulate.mjs`가
  헤드리스로 돌리는 대상. 모든 무작위성은 인자로 받은 `rng`로만 쓴다.
- `js/ai.js` — 장로 의사결정 정책 (시뮬레이터용이자 향후 봇전 모드의 기반)
- `js/data/relics.js` — 유물/이름/증언/스토리 텍스트 (콘텐츠 수정은 여기)
- `js/ui.js` — DOM 렌더 (타이틀/하루 진행/종료 화면)
- `js/net.js` — 온라인 동기화 배관(RTDB, `redmoon_rooms/` 네임스페이스). 현재
  장로 모드는 싱글플레이라 미사용 — 봇전/멀티 모드를 붙일 때 연결
- `js/storage.js` — 새로고침 복원
- `js/audio.js` / `js/palettes.js` — 사운드 엔진과 팔레트(RITUAL) 분리
- `sw.js` — PWA 캐시 (network-first + CACHE_VERSION + controllerchange 1회 reload)
- `scripts/simulate.mjs` — headless 시뮬레이터 (AI/밸런스/라운드 플로우 검증)
- `tools/gen-icons.js` — PWA 아이콘 생성 스크립트

## 게임 규칙 요약 (엔진 로직의 근거)

- 마을 11명 + 유물함(중앙 유물 풀, 초기 3장): 제물(1) / 그림자=하수인(1) / 예지(1) /
  도둑(1) / 문제아(1) / 취객(1) / 광기(1) / 결계(2) / 평범(2). 관련 룰북: 원나잇
  얼티메이트 웨어울프(한국어판) — 단, 아래처럼 "하룻밤 한정"이던 원작 능력들을
  "매일 밤 반복"으로 확장했다.
- **`villager.relic`이 그 사람의 "지금" 정체의 유일한 출처다.** 별도 ID로 위협을
  추적하지 않는다(과거에 `sacrificeHolderId`를 relic과 따로 추적하다가 유물 이동 후
  단서가 의미를 잃는 버그가 났음 — 지금은 relic 필드 자체가 항상 최신이므로 그 문제가
  구조적으로 없다).
- 도둑/문제아/취객은 **매일 밤** 실제로 유물을 맞바꾼다(그날그날 그 유물을 쥔 사람이
  수행 — 캐스케이드 순서는 도둑→문제아→취객, `js/engine.js`의 `cascade*` 함수):
  도둑은 무작위 한 명과 자신의 유물을 통째로 교환, 문제아는 자신을 뺀 다른 두 명의
  유물을 교환(자기 유물은 유지), 취객은 자기 유물을 유물함에 넣고 무작위로 하나 뽑음.
  처형된 사람의 유물도 유물함으로 돌아가 계속 순환한다.
- 매일 처형은 필수. 그날 처형 대상이 `relic==='sacrifice'`가 아니면(광기의 유물
  예외 있음, 아래) 즉시 패배. 투옥은 선택이며 되돌릴 수 있음.
- **감옥은 한 자리뿐**(`jail()`이 새 대상을 가두면 이전 수감자를 자동 석방). 갇힌
  동안은 도둑/문제아/취객의 밤 캐스케이드가 그 사람을 건너뛴다(이미 `!v.jailed`로
  체크 중).
- 제물/하수인의 사칭 대상(`claimRole`)은 처음 그 역할이 됐을 때 한 번만 정해지고
  안 바뀐다(`ensureClaimRoles` — 이미 있으면 건너뜀). 도둑 캐스케이드에 휘말려
  새로 제물/하수인이 된 사람도 다음 밤 페이즈에서 자동으로 커버를 얻는다. 제물은
  살아있는 진짜 유물 중에서, 하수인은 유물함 안에서 훨씬 안일하게 고른다.
- 도둑 캐스케이드로 새 역할을 얻은 사람은 `stoleFrom`(그날 밤만 유효, 매일 초기화)에
  누구 걸 훔쳤는지 기록되고, 그날 자기 주장 뒤에 "사실 어젯밤 도둑의 유물로 OO의
  유물을 훔쳐온 거예요"를 덧붙인다 — 도둑맞은 쪽(지금 `relic==='robber'`인 사람)은
  영문을 모르므로 그런 설명이 없다.
- **제물은 여러 명일 수 있다.** 처형이 진짜 성공이든, 광기의 유물을 처형해 "성공한
  것처럼 보이는" 위장이든 — 그 처형 직후 하수인이 갇혀있지 않으면 무작위 생존자
  한 명의 유물을 `'sacrifice'`로 바꿔(원래 유물은 유물함으로) 제물을 하나 더 심는다.
  광기의 유물은 실제로는 아무 제물도 제거하지 못하면서 장로에게는 성공처럼 보이므로,
  하수인 입장에선 들키지 않고 위협을 늘릴 절호의 기회다.
- 승리: 살아있는 `relic==='sacrifice'` 소지자가 0명이고 하수인이 갇혀있거나 죽었을 때.
  최대 7일 안에 못 끝내면 패배.
- 사냥꾼은 제거했다(투표가 장로 1인뿐이라 "사냥꾼이 지목한 자도 죽는다"는 원작 효과가
  성립하지 않음). 무두장이는 "광기의 유물"로 개명하고 위 위장 메커닉으로 대체했다.

## 명령어

- 로컬 서버: `python3 -m http.server`
- 시뮬: `node scripts/simulate.mjs [판수]` (기본 500판, 교착 0이어야 함)
- 사운드 미리듣기: `audio-preview.html`을 로컬 서버로 열기

## 규칙

- 처음부터 포함: 상대경로, network-first SW, 세션 복원, `redmoon_` DB 네임스페이스
  (나중에 얹으면 매번 같은 버그가 났다 — game-baserule 12개 저장소 교훈)
- 장로 정책(js/ai.js)이나 밤 페이즈 판정 로직을 수정하면 시뮬레이터부터 돌려서
  교착 0 / 승률이 0%나 100%로 붕괴하지 않았는지 확인한 뒤 커밋
- UI 문자열/새 CSS 클래스는 구체적 이름 사용 (일반명 충돌 전례)
- 커밋 전 검증·SW·모바일: webgame-ship 스킬 / 온라인: firebase-online 스킬 참조
  (둘 다 `drayage-kit` 마켓플레이스의 game-kit 플러그인으로 제공됨)

## 아직 안 한 것

- 불면증환자/도플갱어 (나머지 두 원작 역할)
- 봇전 모드 (js/ai.js의 정책을 상대 봇으로 재사용 예정)
- 런 종료 후 메타 진행(장로의 유산) — 로그라이크 언락 시스템
